# Example .env file for deployment to be used with set-gh-secrets.sh as the $ENV_FILE argument in the deploy script.
# Use this file to create a .env (if it doesn't exist) file and fill in the values.
DB_URL=postgresql://postgres:password@localhost:5432/babytrack?sslmode=disable&search_path=babytrack
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=25
# DB_CONN_MAX_LIFETIME=5m
CORS_ALLOWED_ORIGINS=http://localhost:8080
JWT_PUBLIC_KEY_BASE64=enter-public-key-here