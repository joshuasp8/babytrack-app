# Example .env file for local dev. Use this file to create a .env file if one does not exist.
# You can copy this file and rename as .env: `cp .env.template .env`
# While this file contains no prod secrets on its own, we avoid commiting a .env file to prevent build complexity
DB_URL=postgresql://postgres:password@localhost:5432/babytrack?sslmode=disable&search_path=babytrack
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=25
# DB_CONN_MAX_LIFETIME=5m
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:8080
JWT_PUBLIC_KEY_BASE64=enter-public-key-here