{
    "name": "babytrack",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --test tests/*.js",
        "build": "rm -rf dist && mkdir dist && cp -r src/* dist/"
    }
}
